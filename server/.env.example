# Server
PORT=3000

# Database
# DATABASE_URL НЕ се задава тук - идва автоматично от docker-compose.yml,
# построена от POSTGRES_* стойностите в root .env файла (единен източник,
# без ръчно дублиране). Виж docker-compose.yml -> lokum-server -> environment.

# Authentication
JWT_SECRET=
